node_modules/
dist/
.DS_Store
*.log
.vite/

# transient visual-diff output (regenerated by `npm run smoke:diff` on regressions)
demo/shots-diff/

# locally-captured baseline (regenerated by `npm run smoke:baseline`)
demo/shots-baseline/

# Gaussian-splat photo assets for UC4 (SOG files, ~10 MB each, sourced from
# ../../ml-sharp/samples). Not bundled in git — copy from the ml-sharp tree
# into public/spatial/ as part of dataspace setup. See P5d in test-plan.md.
public/spatial/
