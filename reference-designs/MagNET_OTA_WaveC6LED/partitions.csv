# Custom layout for the WaveC6LED. 4 MB flash.
#
# The default single-app table gives the app 1 MB, and D2 already fills 944 KB
# of it once esp_http_client and cJSON link in — 10% free, with mbedTLS still to
# come in D3. Moving this now is cheaper than discovering it mid-phase.
#
# NO ota_0/ota_1 PAIR, deliberately. This device's updates are FORTH BUNDLES —
# source text evaluated into the dictionary — not ESP-IDF app images, so it
# never needs two app slots to swap between. That is what makes a 3 MB single
# app affordable, and it is the whole reason the hot-swap story fits on a 4 MB
# part at all.
#
# storage/ holds downloaded bundles and their signatures (D3).
#
# name,     type, subtype,  offset,    size
nvs,        data, nvs,      0x9000,    0x6000
phy_init,   data, phy,      0xf000,    0x1000
factory,    app,  factory,  0x10000,   0x300000
storage,    data, spiffs,   0x310000,  0xF0000
